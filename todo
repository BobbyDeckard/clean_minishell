1.	Check semicolons, maybe as simple as defining them as the root node
2.	Replace every justified malloc with a calloc
3.	When we remove an arg caused by nonexistent var we should also remove the corresponding exp entry
